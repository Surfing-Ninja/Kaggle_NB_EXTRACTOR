{"metadata":{"kernelspec":{"name":"ir","display_name":"R","language":"R"},"language_info":{"name":"R","codemirror_mode":"r","pygments_lexer":"r","mimetype":"text/x-r-source","file_extension":".r","version":"4.4.0"},"kaggle":{"accelerator":"none","dataSources":[{"sourceId":91722,"databundleVersionId":14262372,"sourceType":"competition"}],"dockerImageVersionId":30749,"isInternetEnabled":true,"language":"r","sourceType":"notebook","isGpuEnabled":false}},"nbformat_minor":4,"nbformat":4,"cells":[{"cell_type":"code","source":"suppressMessages( {\n  #install.packages(\"lightgbm\", quiet = TRUE)\n  library(lightgbm)\n  library(catboost)\n  library(Metrics)\n  library(recipes)\n  library(modelr)\n  library(caret)\n  library(data.table)\n  library(tidyverse)\n  library(MLmetrics)\n  library(ModelMetrics)\n  library(tidyverse)\n})\n\nfree <- function() invisible(gc())\ncalcMode <- function(x) {\n  x <- x[!is.na(x)]\n  ux <- unique(x)\n  ux[which.max(tabulate(match(x, ux)))]\n}\n\ngenerate_combinations <- function(data, operations = c(\"sum\", \"diff\", \"prod\", \"div\")) {\n  numeric_vars <- names(data)[sapply(data, is.numeric)]\n  combinations <- combn(numeric_vars, 2, simplify = FALSE)\n  new_data <- data\n  \n  for (combo in combinations) {\n    var1 <- combo[1]\n    var2 <- combo[2]\n    \n    if (\"sum\" %in% operations) {\n      new_var_name <- paste(var1, \"sum\", var2, sep = \"_\")\n      new_data[[new_var_name]] <- data[[var1]] + data[[var2]]\n    }\n    \n    if (\"diff\" %in% operations) {\n      new_var_name <- paste(var1, \"diff\", var2, sep = \"_\")\n      new_data[[new_var_name]] <- data[[var1]] - data[[var2]]\n    }\n    \n    if (\"prod\" %in% operations) {\n      new_var_name <- paste(var1, \"prod\", var2, sep = \"_\")\n      new_data[[new_var_name]] <- data[[var1]] * data[[var2]]\n    }\n    \n    if (\"div\" %in% operations) {\n      new_var_name <- paste(var1, \"div\", var2, sep = \"_\")\n      new_data[[new_var_name]] <- ifelse(data[[var2]] != 0, data[[var1]] / data[[var2]], NA)\n    }\n  }\n  \n  return(new_data)\n}\navgAUC <- function(real, pred){\n  avgRes <- c()\n  real <- as.matrix(real)\n  for(iPred in 1:ncol(real)){\n    nameCol <- names(real)[iPred]\n    avgRes <- c(avgRes,auc(real[,iPred], pred[,iPred]))\n  }\n  return(mean(avgRes))\n}\n\n# Read files\n#PATH <- \"/kaggle/input/playground-series-s5e11/\"\n#PATH <- \"C:/Users/Axovivas/Documents/Kaggle/playground-series-s5e11/\"\nPATH <- \"/kaggle/input/playground-series-s5e11/\"\ndt <- fread(paste0(PATH, \"train.csv\"))\n#dt2 <- fread(\"/kaggle/input/loan-approval-prediction/credit_risk_dataset.csv\")\n#dt2 <- cbind(id = 1:nrow(dt2),dt2)\n#dt <- rbind(dt, dt2)\n#rm(dt2)\n\ndtest <- fread(paste0(PATH, \"test.csv\"))\nsub <- fread(paste0(PATH,\"sample_submission.csv\"))\nsubTemp <- sub[,2]\n\n# Identify target\ntarget <- names(dt)[!names(dt) %in% names(dtest)] \nY <- dt[,..target]\n#Y <- apply(Y, 1, max)\nY <- Y$loan_paid_back\ndt[,(target):=NULL]\n\n# Merge both sources train and test\ndtTotal <- rbind(data.table(fuente = \"train\",dt),\n                 data.table(fuente = \"test\",dtest))\n\n#dtTotal <- cbind(dtTotal[,1:2],\n#                 generate_combinations(dtTotal[,3:ncol(dtTotal)], operations = c(\"sum\", \"diff\", \"prod\", \"div\"))\n#)\n\n####################################\n######## Feature Engineering #######\n####################################\ndtTotal[, ':='(grade = substr(grade_subgrade,1,1),\n               subgrade = as.integer(substr(grade_subgrade,2,2))\n               )]\ndtTotal[,gradeInt := as.integer(factor(x = grade,levels = c('F','E','D','C','B','A')))]\ndtTotal[,GradeFinal := gradeInt*subgrade]\n\ndtTotal[,employment_status := ifelse(employment_status=='Unemployed',1,0) +\n                              ifelse(employment_status=='Student',2,0) +\n                              ifelse(employment_status=='Employed',3,0) +\n                              ifelse(employment_status=='Self-employed',4,0) +\n                              ifelse(employment_status=='Retired',5,0)\n          ]\n\ndtTotal[,education_level := as.integer(factor(x = education_level,levels = c(\"Bachelor's\",\n                                                                             \"Master's\",\n                                                                             'Other',\n                                                                             'High School',\n                                                                             'PhD'\n                                                                             )))]\n\ndtTotal[,loan_purpose := as.integer(factor(x = loan_purpose,levels = c(\"Education\",\"Medical\",'Vacation',\n                                                                       'Debt consolidation',\n                                                                       'Car', 'Other','Business','Home'\n                                                                        )))]\n\n#dt[,.(.N, mean(loan_paid_back)), by = marital_status][order(V2)]\ndtTotal[,marital_status := as.integer(factor(x = marital_status,levels = c(\"Widowed\",\"Divorced\",\n                                                                           'Single','Married')))]\ndtTotal[,gender := as.integer(factor(x = marital_status,levels = c(\"Other\",\"Male\",'Female')))]\n\nif(1==2){\n  dtTotal[,':=' (income_per_month = annual_income/12,\n                 loan_per_month = (loan_amount*interest_rate/100) / 12 )]\n  \n  dtTotal[,':=' (loan_to_income_ratio = loan_amount/(annual_income+1e-6),\n                 credit_score_to_loan_ratio = credit_score / (loan_amount + 1e-6),\n                 income_after_loan = income_per_month - loan_per_month,\n                 credit_score_x_income = credit_score * annual_income,\n                 income_div_credit_score = annual_income / credit_score,\n                 loan_div_credit_score = loan_amount / credit_score,\n                 credit_score_div_interest_rate = credit_score / interest_rate,\n                 credit_score_x_employment_status = credit_score * employment_status,\n                 credit_score_x_GradeFinal = credit_score * GradeFinal,\n                 GradeFinal_x_employment_status = employment_status* GradeFinal,\n                 credit_score_x_GradeFinal_x_employment_status = credit_score*employment_status* GradeFinal,\n                 interest_rate_div_employment_status = interest_rate / employment_status\n                 #loan_term_x_credit_score = loan_term_months * credit_score\n  )]\n}\n\n\n####################################\n### Elimino variables duplicadas ###\n####################################\ncolsDuplicated <- names(dtTotal)[duplicated(as.list(dtTotal))]\ncolsDuplicated\nif(length(colsDuplicated)>0) {\n  dtTotal[,(colsDuplicated):= NULL]\n}\n\n####################################\n### Elimino variables %Nulls     ###\n####################################\nfCols <- names(dtTotal)\nif(1==1){\n  muchosNulos <- names(dtTotal)[colMeans(is.na(dtTotal))>=0.80]\n  muchosNulos\n  if(length(muchosNulos)>0) {\n    dtTotal[, c(muchosNulos):=NULL]\n  }\n}\n\n####################################\n### Elimino variables one value  ###\n####################################\nfCols <- names(dtTotal)\nnCategories <- dtTotal[, lapply(.SD, function(x) {uniqueN(x)} ), .SDcols = fCols]\nfeaUnicoValor <- fCols[nCategories==1]\nif(length(feaUnicoValor)>0) {\n  dtTotal[, c(feaUnicoValor):=NULL]\n}\n#########################################\n## Agrego cuantas filas nulas por feature\n#########################################\nif(sum(colSums(is.na(dtTotal))>0)>0){\n  dtTotal$FilasNulas <- rowSums(is.na(dtTotal))  \n}\n\n###################################\n###### Features tipo caracter #####\n###################################\nfCols <- names(dtTotal)[3:ncol(dtTotal)]\nfChar <- dtTotal[, lapply(.SD, function(x) {is.character(x)} ), .SDcols = fCols]\nfColsChr <- fCols[fChar==TRUE]\nif(length(fColsChr)>0){\n  dtTotal[, (fColsChr) := lapply(.SD, function(x) { as.integer(factor(x)) } ), .SDcols = fColsChr] \n}\n\n###################################\n### Features con poca variación  ##\n###################################\nColsTotal <- names(dtTotal[,3:ncol(dtTotal)])\niColsBorradas <- 0\nfor(c in ColsTotal){\n  moda <- apply(dtTotal[,..c],2,calcMode)\n  prcRepetido <- mean(dtTotal[,..c]==moda, na.rm=T)\n  if(prcRepetido>= 0.999){\n    cat(paste(\"La variable \",c,\" tiene un mismo valor que se repite \", prcRepetido, \"... va a ser borrada \\n\"))\n    dtTotal[,c(c):=NULL]\n    iColsBorradas <- iColsBorradas + 1\n  }\n}\ncat(paste0(\"\\n Total de columnas borradas \", iColsBorradas),\"\\n\\n\")\n##########################\n\n###################################\n### Frec Encoding para var CAT  ###\n###################################\nif(1==2){\n  fCols <- names(dtTotal)[3:ncol(dtTotal)]\n  nCategories <- dtTotal[, lapply(.SD, function(x) {uniqueN(x)} ), .SDcols = fCols]\n  feaCAT <- fCols[nCategories<=15]\n  \n  for(c in feaCAT){\n    #cat(c)\n    #med <- mean(dtTotal[,..c], na.rm = T)\n    grp <- dtTotal[,..c][, .N,c]\n    dtTotal[grp, paste0(c,\"_FreqEnc\") := N ,on = c]\n  }\n}\n\n##############################\n### Eliminar correlacionados\nif(1==1){\n  correlationMatrix <- cor(dtTotal[,3:ncol(dtTotal)])\n  remove_cols <-\n    findCorrelation(x = correlationMatrix, cutoff = 0.999, verbose = FALSE, names=TRUE)\n  if(length(remove_cols)>0){\n    dtTotal[,c(remove_cols):=NULL] \n    print(remove_cols)\n  }\n}\n##############################\n## outliers\n##############################\nif(1==1){\n  fCols <- names(dtTotal)[3:ncol(dtTotal)]\n  q1Features <- apply(dtTotal[,..fCols], 2, function(x) {quantile(x,0.25)})\n  q3Features <- apply(dtTotal[,..fCols], 2, function(x) {quantile(x,0.75)})\n  IQRFeatures <- q3Features - q1Features\n  OutliersLeft <- q1Features - IQRFeatures*1.5\n  OutliersRight <- q3Features + IQRFeatures*1.5\n  nOutliersLeft <- apply(dtTotal[,..fCols], 1, function(x) { sum(x <= OutliersLeft)}) \n  nOutliersRight <- apply(dtTotal[,..fCols], 1, function(x) { sum(x >= OutliersRight)})\n  nOutliers <- nOutliersLeft + nOutliersRight\n  #dtTotal[,nOutliersLeft := nOutliersLeft]\n  #dtTotal[,nOutliersRight := nOutliersRight]\n  dtTotal[,nOutliers := nOutliers]    \n}\n\n###################################\n####### Tratamiento Nulos   #######\n###################################\nif(1==1){\n  ColsNulls <- names(dtTotal)[colSums( is.na(dtTotal))>0]\n  for(c in ColsNulls){\n    #cat(c)\n    #med <- mean(dtTotal[,..c], na.rm = T)\n    #med <- apply(dtTotal[,..c],2,mean,na.rm=T)\n    med <- apply(dtTotal[,..c],2,median,na.rm=T)\n    #med <- 0\n    #med <- apply(dtTotal[,..c],2,median,na.rm=T)\n    dtTotal[, (c) := lapply(.SD, function(x) { ifelse(is.na(x),med,x)  } ), .SDcols = c]\n  }\n}\n\n####################################\n### Feature Engineering ###\n####################################\n#dtTotal[,loan_to_income := (loan_amnt/person_income) - loan_percent_income]\n\n\n### divido ambos dataset de nuevo\ndt <- dtTotal[fuente == 'train', ]\ndtest <- dtTotal[fuente == 'test', ]\n\nrec <- dt %>%\n  recipe(~ .) %>%\n  step_rm(id) %>% \n  step_rm(fuente) %>% \n  step_corr(all_numeric(), threshold = .98) %>% \n  #step_BoxCox(all_numeric()) %>%\n  step_normalize(all_numeric()) %>%\n  prep()\n\ndt <- juice(rec, composition = \"data.frame\")\ndtest <- bake(rec, dtest, composition = \"data.frame\")\nsetDT(dt)\nsetDT(dtest)","metadata":{"_uuid":"051d70d956493feee0c6d64651c6a088724dca2a","_execution_state":"idle","trusted":true,"execution":{"iopub.status.busy":"2025-11-08T22:28:18.04667Z","iopub.execute_input":"2025-11-08T22:28:18.077946Z","iopub.status.idle":"2025-11-08T22:28:26.118313Z","shell.execute_reply":"2025-11-08T22:28:26.116515Z"}},"outputs":[],"execution_count":null},{"cell_type":"code","source":"oof <- data.frame(target = Y)\n#dt[,c(\"fuente\", \"id\"):=NULL]\n#dtest[,c(\"fuente\", \"id\"):=NULL]\nsubSeeds <- sub\nsetDF(subSeeds)\nsubSeeds[,-1] <- 0\nfree()\n\n\nscores <- c()\nSEEDS <- c(2503)\ns <- SEEDS\nSEEDS <- c(1975, 2000, 2503,1511,2604)\nfor (s in SEEDS) {\n  \n  set.seed(s)\n  nameSeed <- paste0(\"Seed_\",s)\n  subSeeds[, nameSeed] <- 0\n  \n  kfolds = 10\n  set.seed(s)\n  folds <- createFolds(Y, k = kfolds, list = FALSE)\n  pred <- rep(0,nrow(sub))\n  predCB_Total <- pred\n  predLGBM_Total <- pred\n  predXGB_Total <- pred\n  \n  aucCBTotal <- 0\n  aucLGBMTotal <- 0\n  aucXGBTotal <- 0\n  aucTotal <- 0\n  auc_LGBM <- 0\n  \n  #dtMatrix <- data.matrix(dt)\n  #dtestMatrix <- data.matrix(dtest)\n  #rm(dt, dtest); free()\n  iFold <- 1\n  #feaCategories <- c(\"hypertension\", \"heart_disease\", \"ever_married\", \n  #                   \"work_type\", \"Residence_type\", \"smoking_status\")\n  for (iFold in 1:kfolds)\n    #for (iFold in 1:1)\n  {\n    cat(\"\\n CatBoost Fold \",iFold, \"-\", kfolds, \"\\n\")\n    \n    idx <- which(folds!=iFold)\n    \n    ############################\n    ######## CATBOOST ##########\n    ############################  \n    #feaCatsInt <- which(names(dt) %in% feaCats)\n    \n    testpool <- catboost.load_pool(dt[-idx, ], label = Y[-idx]) ##, cat_features = feaCatsInt)\n    trainpool <- catboost.load_pool(dt[idx, ], label = Y[idx]) ##, cat_features = feaCatsInt)\n    \n    \n    params <- list(iterations = 2000, eval_metric = 'AUC', task_type = 'CPU', random_seed = s, \n                   metric_period=250, ##learning_rate = 0.001\n                   loss_function = 'Logloss',\n                   verbose = 1, use_best_model = T, od_type = 'Iter', od_wait = 250)\n    \n    cat_model <- catboost.train( learn_pool = trainpool, test_pool = testpool, params = params)\n    testCB <- catboost.predict(cat_model, testpool, prediction_type = 'Probability')\n    nameSeed <- paste0(\"CB_Seed_\",s)\n    #oof[-tri, nameSeed] <- result\n    #oof[-idx, \"CB\"] <- testCB\n    oof[-idx, nameSeed] <- testCB\n    \n    AUC_CB <- auc(Y[-idx], testCB)\n    aucCBTotal <- aucCBTotal + AUC_CB\n    scores <- c(scores,AUC_CB)\n    #cat(\"\\n CatBoost \\n\")\n    #print(AUC_CB)\n    #cat(\"\\n CatBoost \", AUC_CB, \"AvgMetrics\", aucCBTotal/iFold, \"\\n\")\n    cat(\"\\n CatBoost - SEED\",s, \" Fold \",iFold, \"-\", kfolds , \"metric \", AUC_CB, \" AvgMetrics\", mean(scores), \"\\n\")\n    \n    predCB <- catboost.predict(cat_model, catboost.load_pool(dtest),prediction_type = 'Probability')\n    predCB_Total <- predCB_Total + predCB\n    subTemp[, paste0(\"Seed_s\",s,\"Fold_\",iFold)] <- predCB\n    \n    nameSeed <- paste0(\"Seed_\",s)\n    subSeeds[, nameSeed] <- subSeeds[, nameSeed] + predCB/ kfolds \n    \n    ##predCB <- catboost.predict(cat_model, catboost.load_pool(dtest))\n    if (1==2) {\n      dImp <- catboost.get_feature_importance(cat_model, \n                                              pool = testpool, \n                                              type = 'FeatureImportance',\n                                              thread_count = -1)\n      dImp <- as.data.frame(dImp)\n      View(arrange(dImp,-V1))\n      write.csv(dImp, \"ImportanciaCB_v1.csv\")\n    } \n    ############################\n    ############################\n  }\n}\n\npredCB_Total <- predCB_Total/kfolds/length(SEEDS)\n#cat(\"AUC CB\", aucCBTotal/kfolds,\"\\n\")\ncat(\"AUC CB\", mean(scores),\"\\n\")\n\nsub[,2]  <- apply(subTemp[,2:ncol(subTemp)],1,mean)\nfwrite(sub, \"subCBFit_v1.csv\")\n\nsub[,2]  <- apply(subTemp[,2:ncol(subTemp)],1,median)\nfwrite(sub, \"subCBFitMedian_v1.csv\")\n\nfwrite(subTemp, \"subFoldLGBMFit_v1.csv\")\nfwrite(oof, \"OOF_CBFit_v1.csv\")\n\nfwrite(subSeeds,\"subSeedsCB_v1.csv\") #-- 3 Envío\n\n#print(auc(oof$target, oof$CB))\nprint(auc(oof$target, oof$CB_Seed_1975))\nprint(auc(oof$target, oof$CB_Seed_2000))\nprint(auc(oof$target, oof$CB_Seed_2503))\nprint(auc(oof$target, oof$CB_Seed_1511))\nprint(auc(oof$target, oof$CB_Seed_2604))\n\naucMean <-( (auc(oof[,1], oof[,2]) + auc(oof[,1], oof[,3]) + auc(oof[,1], oof[,4]) + \n               auc(oof[,1], oof[,5]) + auc(oof[,1], oof[,6]))/5)\ncat(\"Metric mean \", aucMean)","metadata":{"trusted":true,"execution":{"iopub.status.busy":"2025-11-08T22:28:26.1216Z","iopub.execute_input":"2025-11-08T22:28:26.122902Z","iopub.status.idle":"2025-11-08T23:17:03.763045Z","shell.execute_reply":"2025-11-08T23:17:03.752996Z"}},"outputs":[],"execution_count":null}]}